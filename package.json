{
  "name": "lonewriter-monorepo",
  "version": "2.0.0-timeline",
  "private": true,
  "description": "LoneWriter Project Monorepo (App + Landing)",
  "scripts": {
    "install:app": "npm install --prefix app",
    "dev": "npm run dev --prefix app",
    "build": "npm run build --prefix app",
    "preview": "npm run preview --prefix app",
    "lint": "npm run lint --prefix app"
  },
  "author": "Sergio Sánchez",
  "license": "MIT"
}
